import './index.scss'
const NotificationComponent = (props) => {
    return(
        <div className="notification-wrapper">
                <p>{language.header.notification.text}</p>
        </div>
    );
};

export default NotificationComponent;